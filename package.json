{
  "name": "mealbandit",
  "version": "0.1.0",
  "private": true,
  "description": "Static recipe blueprint builder with preference-aware macros and optional Grok meal photos.",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node server.js",
    "test": "playwright test",
    "test:live": "playwright test --config=playwright.live.config.js",
    "test:smoke": "playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
